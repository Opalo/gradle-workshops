apply plugin: 'idea'
apply plugin: 'groovy'

buildDir = 'different'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile gradleApi()
    testCompile 'org.spockframework:spock-core:0.7-groovy-1.8'
}

// Specification
// Define, a field named 'filteredContent' that will match all *.txt files (including subdirs) from 'content' directory
// Define 3 tasks:
// - 'showFiles' that will print to standard output files from 'fileteredContent' field
// - 'copyFiles' of type 'Copy' that will copy files from 'filteredContent' to 'files' folder under buildDir
// - 'copyTree' of type 'Copy' that will copy files from 'filteredContent' to 'fileTree' folder under buildDir preserving directory structure

// Your solution goes below
